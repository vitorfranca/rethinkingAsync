$(document).ready(function(){
	var $btn = $("#btn"),
		$list = $("#list");

	// var clicks = ??
	// var msgs;

	$btn.click(function(evt){
		// hint: push()
	});

	// TODO: setup sampled sequence, populate $list

	// hint: setInterval
	// hint: $list.append(msg + "<br>");
});
